import datetime
import requests
import pandas as pd
from bs4 import BeautifulSoup
from zoneinfo import ZoneInfo


def load_matchups(target_date: datetime.date):
    # [... scraped code from original] ...
    # return df, best_df, display_date, date_str
    return df, best_df, display_date, date_str
