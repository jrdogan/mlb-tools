from jinja2 import Environment, FileSystemLoader
import os

def build_index():
    # read repo contents, bucket them, return rendered HTML string
    return html

def commit_index(html):
    # push index.html via PyGithub
    pass
